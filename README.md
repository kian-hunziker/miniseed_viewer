
# Setup
Create a conda environment, we recommend python 3.9.
Activate the environment and install requirements.
Run viewer.py.

```bash
conda create --name viewer python=3.9
conda activate viewer
pip install -r requirements.txt
python3 viewer.py
```